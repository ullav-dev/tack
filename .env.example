API_URL=http://localhost:8087   # server-side only (tack-server)
AUTH_URL=http://localhost:8081  # server-side only (ullav-user-management)
NEXT_PUBLIC_IDLE_TIMEOUT_MS=3600000  # optional, default 1 hour
NEXT_PUBLIC_HOCUSPOCUS_URL=ws://localhost:8088  # tack-hocuspocus -- connected to directly from the browser, NOT proxied via /api (WebSocket upgrades don't reliably survive Next.js middleware rewrites)
